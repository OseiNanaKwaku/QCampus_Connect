services:
  validator:
    image: hyperledger/besu:24.12.2
    container_name: besu-validator
    restart: unless-stopped
    volumes:
      - ./genesis.json:/opt/besu/genesis.json:ro,z
      - ./keys/validator.key:/opt/besu/keys/validator.key:ro,z
      - ./static-nodes.json:/var/lib/besu/data/static-nodes.json:ro,z
      - validator-data:/var/lib/besu/data
    ports:
      - "8545:8545"
    environment:
      - BESU_LOGGING=INFO
    command:
      - --genesis-file=/opt/besu/genesis.json
      - --data-path=/var/lib/besu/data
      - --node-private-key-file=/opt/besu/keys/validator.key
      - --discovery-enabled=false
      - --p2p-host=172.28.0.10
      - --p2p-port=30303
      - --sync-mode=FULL
      - --sync-min-peers=1
      - --min-gas-price=0
      - --rpc-http-enabled=true
      - --rpc-http-host=0.0.0.0
      - --rpc-http-port=8545
      - --rpc-http-api=ETH,NET,WEB3,QBFT
      - --rpc-http-cors-origins=*
      - --host-allowlist=*
    networks:
      besu-net:
        ipv4_address: 172.28.0.10

  peer:
    image: hyperledger/besu:24.12.2
    container_name: besu-peer
    restart: unless-stopped
    volumes:
      - ./genesis.json:/opt/besu/genesis.json:ro,z
      - ./keys/peer.key:/opt/besu/keys/peer.key:ro,z
      - ./static-nodes.json:/var/lib/besu/data/static-nodes.json:ro,z
      - peer-data:/var/lib/besu/data
    environment:
      - BESU_LOGGING=INFO
    command:
      - --genesis-file=/opt/besu/genesis.json
      - --data-path=/var/lib/besu/data
      - --node-private-key-file=/opt/besu/keys/peer.key
      - --discovery-enabled=false
      - --p2p-host=172.28.0.11
      - --p2p-port=30303
      - --sync-mode=FULL
      - --sync-min-peers=1
      - --min-gas-price=0
      - --rpc-http-enabled=false
    networks:
      besu-net:
        ipv4_address: 172.28.0.11

volumes:
  validator-data:
  peer-data:

networks:
  besu-net:
    driver: bridge
    ipam:
      config:
        - subnet: 172.28.0.0/16
